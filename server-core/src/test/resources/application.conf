# Only what the tests need: the TLS connector and the keystore belong to serverApp, which owns the engine.
ktor {
    application {
        modules = [ com.pttlan.server.ServerModuleKt.module ]
    }
}

ptt {
    adminPassword = ${?PTT_ADMIN_PASSWORD}
    trustProxy = false
    trustProxy = ${?PTT_TRUST_PROXY}
}
